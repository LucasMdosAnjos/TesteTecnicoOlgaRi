import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:olga_ri/app/modules/pre_login/pre_login_module.dart';

void main() {
  initModule(PreLoginModule());
  // PreLoginController prelogin;
  //
  setUp(() {
    //     prelogin = PreLoginModule.to.get<PreLoginController>();
  });

  group('PreLoginController Test', () {
    //   test("First Test", () {
    //     expect(prelogin, isInstanceOf<PreLoginController>());
    //   });

    //   test("Set Value", () {
    //     expect(prelogin.value, equals(0));
    //     prelogin.increment();
    //     expect(prelogin.value, equals(1));
    //   });
  });
}
